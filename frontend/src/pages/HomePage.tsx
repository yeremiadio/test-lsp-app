import React from "react";
import Layout from "../components/Layouts/Layout";

type Props = {};

const HomePage = (props: Props) => {
  return (
    <Layout>
      <div className="bg-section">Dashboard</div>
    </Layout>
  );
};

export default HomePage;
